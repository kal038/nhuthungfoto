info:
  name: GET CREDITS BALANCE
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8787/v1/credits/balance
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
