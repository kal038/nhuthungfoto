info:
  name: GET CREDIT HISTORY
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8787/v1/credits/history
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
