info:
  name: GET IS ADMIN
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8787/v1/admin/me
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
