info:
  name: GET MODULES - ALL
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8787/v1/modules
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
