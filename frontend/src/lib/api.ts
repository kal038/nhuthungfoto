/**
 * Base API client.
 *
 * Configure the shared fetch/Axios wrapper here.
 * This module should handle base URL, auth tokens, and error mapping
 * so that individual React Query hooks stay thin.
 */
export {}
