---
description: Build the frontend and backend for production
---
This workflow creates optimized production builds for both parts of the stack.

// turbo
1. Run `cd frontend && npm run build` to build the Vite frontend (output in `frontend/dist`).
// turbo
2. Run `cd backend-node && npm run build` to build the backend (output in `backend-node/dist`).
