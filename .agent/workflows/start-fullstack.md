---
description: Start the full-stack development environment (frontend + backend)
---
This workflow starts both the frontend and backend development servers concurrently.

// turbo
1. Run `make up` from the project root. This command will install dependencies if needed, start both frontend (Vite) and backend (Express) dev servers using tmux or background processes, and open the app in your local browser.
