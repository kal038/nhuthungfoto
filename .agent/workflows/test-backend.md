---
description: Run the backend unit tests
---
This workflow runs the backend Vitest test suite.

// turbo
1. Run `cd backend-node && npm run test` to execute the tests.
2. Review the test output in the terminal.
