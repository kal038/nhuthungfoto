---
description: Start the frontend development server only
---
This workflow starts only the React + Vite frontend development server.

// turbo
1. Run `cd frontend && npm run dev` to start the frontend dev server.
