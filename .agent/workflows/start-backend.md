---
description: Start the backend development server only
---
This workflow starts only the Node.js Express backend development server in watch mode.

// turbo
1. Run `cd backend-node && npm run dev` to start the backend dev server.
