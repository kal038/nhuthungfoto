---
description: Run static analysis and linting across the entire project
---
This workflow runs ESLint for both the frontend and backend sequentially.

// turbo
1. Run `cd frontend && npm run lint` to lint the frontend React code.
// turbo
2. Run `cd backend-node && npm run lint` to lint the backend Express code.
